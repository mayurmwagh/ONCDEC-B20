apiVersion: v1 
kind: Pod
metadata:
  name: my-app
  labels:
    app: my-app
spec:
  containers:
    - name: my-container
      image: httpd:latest 
      ports:
        - name: http
          containerPort: 80
    - name: container-1
      image: nginx:latest 
      ports:
        - name: http
          containerPort: 8080
